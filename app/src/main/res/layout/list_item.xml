<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/DateListItem"
        android:layout_width="90dp"
        android:layout_height="55dp"
        android:text="Дата"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/TypeListItem"
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:layout_marginStart="89dp"
        android:text="Тип"
        app:layout_constraintBaseline_toBaselineOf="@+id/DateListItem"
        app:layout_constraintEnd_toStartOf="@+id/SumListItem"
        app:layout_constraintStart_toStartOf="parent" />

    <TextView
        android:id="@+id/SumListItem"
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:text="Сумма"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/TypeListItem"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>